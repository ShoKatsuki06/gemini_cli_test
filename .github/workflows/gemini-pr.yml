# .github/workflows/gemini-pr.yml

name: Simple Test Workflow

on:
  workflow_dispatch: # 手動実行トリガーのみ

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "This is a simple test workflow."
